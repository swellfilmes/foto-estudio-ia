import LandingPage from "@/components/LandingPage";

// A landing de marketing continua viva aqui (ex.: campanhas / anúncios que
// precisam de uma página de venda). A porta principal do site (/) agora é a
// própria plataforma. O formulário da landing leva pra dentro, como antes.
export default function LandingRoute() {
  return <LandingPage />;
}
